diff -Naur joplin/packages/app-desktop/commands/copyDevCommand.ts joplin-patched/packages/app-desktop/commands/copyDevCommand.ts
--- joplin/packages/app-desktop/commands/copyDevCommand.ts	2026-04-30 10:44:07.092676812 +0000
+++ joplin-patched/packages/app-desktop/commands/copyDevCommand.ts	2026-04-30 10:50:27.775205853 +0000
@@ -1,7 +1,6 @@
 import { CommandRuntime, CommandDeclaration } from '@joplin/lib/services/CommandService';
 import { _ } from '@joplin/lib/locale';
 import shim, { MessageBoxType } from '@joplin/lib/shim';
-const app = require('@electron/remote').app;
 const { clipboard } = require('electron');
 
 export const declaration: CommandDeclaration = {
@@ -12,7 +11,7 @@
 export const runtime = (): CommandRuntime => {
 	return {
 		execute: async () => {
-			const appPath = app.getPath('exe');
+			const appPath = process.env.SNAP_INSTANCE_NAME;
 			// Quote the path so it works when it contains spaces (e.g. "C:\Program Files\Joplin\Joplin.exe" on Windows)
 			const cmd = `"${appPath}" --env dev`;
 			clipboard.writeText(cmd);
